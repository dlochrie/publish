describe('post model test', function() {
  it('should inherit from the base model class', function(done) { done(); });
  it('should provide default values', function(done) { done(); });
  it('should provide a model associations object', function(done) { done(); });
  it('should provide a list of select columns', function(done) { done(); });
  it('should provide a model structure', function(done) { done(); });
});
